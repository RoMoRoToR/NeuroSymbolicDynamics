# Tables

This directory is reserved for generated tabular outputs derived from aggregated experiment results.

The canonical builder `scripts/build_figures_tables.py` produces:

- CSV summary tables
- LaTeX tables

If journal-specific formatting is required, it should be applied downstream to these generated artifacts.
